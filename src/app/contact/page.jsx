import React from 'react'

// metadata is for SEO purposes and it can be in every component, title will be shown in document title
export const metadata = {
    title: 'contact page',
    description: 'its a description for contact page in next js project',
  }
function contactPage() {
  return (
    <div>contactPage in contact component</div>
  )
}

export default contactPage